#include "main.h"
/**
 * _memset -write function that fill memory of constant bytes
 *
 * Return: memory area
 */

char *_memset(char *s, char b, unsigned int n);
{
	unsigned int n;

	for (n = 0; n <= 4; n++)
	{
		*(s + 1) = b
	}
	return (s)
}
